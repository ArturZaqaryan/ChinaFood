@using Microsoft.AspNetCore.Mvc.Localization
@inject IViewLocalizer Localizer
@inject IHtmlLocalizer<CommonResources> SharedLocalizer
@model List<(CartItem, DishModel)>
<div class="container search-results">
    <div class="content__column">
        <article class="article">
            <div class="article__title title">
                <h3>Your cart</h3>
            </div>
           @*  <div class="article__row">
                @foreach (var item in Model)
                {
                    <div class="article__card card" style="background-image: url(../@item.Item2.TitleImagePath);">
                        <div class="card__row card" style="background-image: url(../@item.Item2.TitleImagePath);">
                            <div class="card__name">
                                <p>@item.Item2.Title</p>
                                <p>@item.Item2.Subtitle</p>
                            </div>
                            <div class="card__price">
                                @item.Item2.Price.ToString("C");
                            </div>
                        </div>
                    </div>
                }
            </div> *@
            <div class="cart">
                <table class="table">
                    <thead>
                        <tr>
                            <th>Product</th>
                            <th>Price</th>
                            <th>Quantity</th>
                            <th>Total</th>
                            <th>Actions</th>
                        </tr>
                    </thead>
                    <tbody>
                        <!-- Здесь будут динамически добавляться товары -->
                    </tbody>
                </table>
                <div class="cart-total">
                    Total: <span id="cart-total">0</span>
                </div>
                <button id="checkout-button">Proceed to Checkout</button>
            </div>

            <!-- Форма оформления заказа -->
            <div class="checkout-form" style="display: none;">
                <h3>Checkout</h3>
                <form id="checkout-form">
                    <label for="name">Name</label>
                    <input type="text" id="name" name="name" required>
                    <label for="email">Email</label>
                    <input type="email" id="email" name="email" required>
                    <label for="address">Address</label>
                    <textarea id="address" name="address" required></textarea>
                    <button type="submit">Submit Order</button>
                </form>
            </div>
        </article>
    </div>
</div>

